From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: SteelMC contributors <noreply@steelmc.dev>
Date: Mon, 10 Aug 2026 00:00:00 +0000
Subject: [PATCH] Use Steel remote NOISE

Selected fresh BIOMES chunks complete NOISE only after an asynchronous V1
artifact has been fully validated, prepared off-chunk, and atomically
committed. Unsupported selected-world contexts fail closed.

diff --git a/net/minecraft/world/level/chunk/status/ChunkStatusTasks.java b/net/minecraft/world/level/chunk/status/ChunkStatusTasks.java
--- a/net/minecraft/world/level/chunk/status/ChunkStatusTasks.java
+++ b/net/minecraft/world/level/chunk/status/ChunkStatusTasks.java
@@ -87,21 +87,29 @@
     ) {
         ServerLevel level = context.level();
         WorldGenRegion region = new WorldGenRegion(level, chunks, step, chunk);
-        return context.generator()
-            .fillFromNoise(Blender.of(region), level.getChunkSource().randomState(), level.structureManager().forWorldGenRegion(region), chunk)
-            .thenApply(generatedChunk -> {
-                if (generatedChunk instanceof ProtoChunk protoChunk) {
-                    BelowZeroRetrogen belowZeroRetrogen = protoChunk.getBelowZeroRetrogen();
-                    if (belowZeroRetrogen != null) {
-                        BelowZeroRetrogen.replaceOldBedrock(protoChunk);
-                        if (belowZeroRetrogen.hasBedrockHoles()) {
-                            belowZeroRetrogen.applyBedrockMask(protoChunk);
-                        }
+        Blender blender = Blender.of(region);
+        CompletableFuture<ChunkAccess> generated;
+        if (io.papermc.paper.worldgen.steel.SteelRemoteNoise.handles(level)) {
+            generated = io.papermc.paper.worldgen.steel.SteelRemoteNoise.generateNoise(
+                level, context.generator(), blender, region.getCenter(), chunk
+            );
+        } else {
+            generated = context.generator()
+                .fillFromNoise(blender, level.getChunkSource().randomState(), level.structureManager().forWorldGenRegion(region), chunk);
+        }
+        return generated.thenApply(generatedChunk -> {
+            if (generatedChunk instanceof ProtoChunk protoChunk) {
+                BelowZeroRetrogen belowZeroRetrogen = protoChunk.getBelowZeroRetrogen();
+                if (belowZeroRetrogen != null) {
+                    BelowZeroRetrogen.replaceOldBedrock(protoChunk);
+                    if (belowZeroRetrogen.hasBedrockHoles()) {
+                        belowZeroRetrogen.applyBedrockMask(protoChunk);
                     }
                 }
-
-                return (ChunkAccess)generatedChunk;
-            });
+            }
+
+            return generatedChunk;
+        });
     }
 
     public static CompletableFuture<ChunkAccess> generateSurface(
diff --git a/net/minecraft/server/dedicated/DedicatedServer.java b/net/minecraft/server/dedicated/DedicatedServer.java
--- a/net/minecraft/server/dedicated/DedicatedServer.java
+++ b/net/minecraft/server/dedicated/DedicatedServer.java
@@ -237,6 +237,7 @@
         // Paper start - initialize global and world-defaults configuration
         this.paperConfigurations.initializeGlobalConfiguration(this.registryAccess());
         this.paperConfigurations.initializeWorldDefaultsConfiguration(this.registryAccess());
+        io.papermc.paper.worldgen.steel.SteelRemoteNoise.start(this); // SteelMC - remote NOISE
         // Paper end - initialize global and world-defaults configuration
         this.server.spark.enableEarlyIfRequested(); // Paper - spark
         // Paper start - fix converting txt to json file; convert old users earlier after PlayerList creation but before file load/save
diff --git a/net/minecraft/server/MinecraftServer.java b/net/minecraft/server/MinecraftServer.java
--- a/net/minecraft/server/MinecraftServer.java
+++ b/net/minecraft/server/MinecraftServer.java
@@ -1142,6 +1142,7 @@
     }
     public void stopPart2() {
         // Folia end - region threading
+        io.papermc.paper.worldgen.steel.SteelRemoteNoise.closeInstance(); // SteelMC - remote NOISE
         this.savedDataStorage.close();
         this.resources.close();
 
diff --git a/ca/spottedleaf/moonrise/patches/chunk_system/scheduling/task/ChunkUpgradeGenericStatusTask.java b/ca/spottedleaf/moonrise/patches/chunk_system/scheduling/task/ChunkUpgradeGenericStatusTask.java
--- a/ca/spottedleaf/moonrise/patches/chunk_system/scheduling/task/ChunkUpgradeGenericStatusTask.java
+++ b/ca/spottedleaf/moonrise/patches/chunk_system/scheduling/task/ChunkUpgradeGenericStatusTask.java
@@ -128,6 +128,10 @@
 
     private void completeClaimed(final ChunkAccess chunk, final Throwable throwable, final boolean generation) {
         final int previous = this.completionGate.claimCompletion();
+        for (io.papermc.paper.worldgen.steel.SteelRemoteNoise.CancellationReservation reservation;
+             (reservation = this.steelCancellationReservations.poll()) != null;) {
+            io.papermc.paper.worldgen.steel.SteelRemoteNoise.clearCancellationBeforeAdmission(reservation);
+        }
         if (previous == CompletionGate.COMPLETING) {
             return;
         }
@@ -191,6 +195,8 @@
     }
 
     private final CompletionGate completionGate = new CompletionGate();
+    private final java.util.concurrent.ConcurrentLinkedQueue<io.papermc.paper.worldgen.steel.SteelRemoteNoise.CancellationReservation>
+            steelCancellationReservations = new java.util.concurrent.ConcurrentLinkedQueue<>();
     private volatile boolean scheduled;
     private static final VarHandle SCHEDULED_HANDLE = ConcurrentUtil.getVarHandle(ChunkUpgradeGenericStatusTask.class, "scheduled", boolean.class);
 
@@ -215,7 +221,31 @@
 
     @Override
     public void cancel() {
+        io.papermc.paper.worldgen.steel.SteelRemoteNoise.CancellationReservation reservation = null;
+        if (this.toStatus == ChunkStatus.NOISE && io.papermc.paper.worldgen.steel.SteelRemoteNoise.handles(this.world)) {
+            final io.papermc.paper.worldgen.steel.SteelRemoteNoise.CancellationResult remote =
+                    io.papermc.paper.worldgen.steel.SteelRemoteNoise.cancel(this.world, this.chunkX, this.chunkZ);
+            if (remote == io.papermc.paper.worldgen.steel.SteelRemoteNoise.CancellationResult.TERMINAL) {
+                return;
+            }
+            if (remote == io.papermc.paper.worldgen.steel.SteelRemoteNoise.CancellationResult.NOT_FOUND) {
+                reservation = io.papermc.paper.worldgen.steel.SteelRemoteNoise.reserveCancellationBeforeAdmission(
+                        this.world, this.chunkX, this.chunkZ
+                );
+                if (reservation != null) {
+                    if (reservation.result() == io.papermc.paper.worldgen.steel.SteelRemoteNoise.CancellationReservationResult.PENDING
+                        || reservation.result() == io.papermc.paper.worldgen.steel.SteelRemoteNoise.CancellationReservationResult.TERMINAL) {
+                        return;
+                    }
+                    this.steelCancellationReservations.add(reservation);
+                }
+            }
+        }
         if (!this.completionGate.requestCancellation()) {
+            if (this.completionGate.state() == CompletionGate.COMPLETING && reservation != null) {
+                this.steelCancellationReservations.remove(reservation);
+                io.papermc.paper.worldgen.steel.SteelRemoteNoise.clearCancellationBeforeAdmission(reservation);
+            }
             return;
         }
         if (this.generateTask.cancel()) {
