# Historical observations

The JSON files in this directory are superseded development-host observations. They predate the current source digest, fatal-shutdown hardening, and maintained internal Folia importer. They are retained only as historical architecture/performance context and must not be cited as current-tree, native-importer, deterministic-final, or production-capacity evidence. Generate fresh hash-linked evidence with `../run-e2e.sh` after a clean committed code freeze.
